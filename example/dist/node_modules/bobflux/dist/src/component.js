define(["require", "exports", 'node_modules/bobril/index', 'node_modules/fun-model/dist/src/index'], function (require, exports, b, index_1) {
    function createComponent(component) {
        return function (c, d, ch) { return b.createDerivedComponent(b.createComponent({
            init: function (ctx) {
                ctx.state = index_1.getState(c);
            },
            shouldChange: function (ctx, me, oldMe) {
                var currentState = index_1.getState(c);
                if (currentState === ctx.state)
                    return false;
                ctx.state = currentState;
                return true;
            }
        }), component)(d, ch); };
    }
    exports.createComponent = createComponent;
});
